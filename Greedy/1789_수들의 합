import sys; input = sys.stdin.readline
S = int(input())
sum = 0
if S == 1:
    print(1)
else:
    for i in range(1, S+1):
        sum += i
        if sum > S:
            print(i-1)
            break
